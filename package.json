{
  "name": "weekly-daily-stars",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Weekly and daily GitHub star reports for Skills and MCP repositories.",
  "scripts": {
    "check": "node --check src/repositories.mjs && node --check src/update-stars.mjs && node --check src/report.mjs",
    "update": "node src/update-stars.mjs --window all",
    "update:daily": "node src/update-stars.mjs --window daily",
    "update:weekly": "node src/update-stars.mjs --window weekly",
    "report": "node src/report.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
